# xiv.today

Event planner for Final Fantasy XIV community events.

## Workspace layout

- `apps/web` contains the xiv.today Next.js application.
- `packages/next-request` contains shared request, server action, route, and cookie utilities for Next.js applications.

Set the xiv.today Vercel project's Root Directory to `apps/web`.
